from setuptools import find_packages, setup


setup(
    name="minilearn",
    version="0.1.0",
    packages=find_packages(),
    description="A custom ML library built from scratch",
    author="Aymen",
)
